<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SignUp">

    <!-- BACKGROUND IMAGE (must be first child so it is behind everything) -->
    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/sign_up_and_sign_in_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Lottie centered -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/sign_up_lottie"
        android:layout_width="250dp"
        android:layout_height="180dp"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:lottie_fileName="signup.json"
        app:lottie_autoPlay="true"
        app:lottie_loop="true" />

    <!-- Heading -->
    <TextView
        android:id="@+id/sign_up_heading"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/sign_up_lottie"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="16dp"
        android:layout_marginEnd="16dp"
        android:text="Sign Up Here"
        android:gravity="center"
        android:textSize="22sp"
        android:fontFamily="@font/inter18ptsemibold"
        android:textColor="@color/primaryText"/>

    <!-- Card container (semi-transparent so blurred bg shows through) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/sign_up_detail_field"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/sign_up_heading"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"
        app:cardCornerRadius="20dp"
        app:cardElevation="6dp"
        app:strokeColor="@color/outline"
        app:strokeWidth="1dp"
        android:padding="16dp"
        app:cardBackgroundColor="#B3FFFFFF">  <!-- 70% white -->

        <LinearLayout
            android:background="@color/sign_up_sign_in_bckground"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp"
            android:orientation="vertical">

            <!-- Email Text Field -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/email_field"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:boxBackgroundMode="outline"
                app:boxCornerRadiusTopStart="12dp"
                app:boxCornerRadiusTopEnd="12dp"
                app:boxCornerRadiusBottomStart="12dp"
                app:boxCornerRadiusBottomEnd="12dp"
                app:boxStrokeColor="@color/outline"
                android:layout_marginBottom="12dp"
                app:hintTextColor="@color/hint_color"
                android:hint="@string/hint_email">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/email_entry_field"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:inputType="textEmailAddress"
                    android:paddingStart="12dp"
                    android:drawableStart="@drawable/email"
                    android:drawablePadding="10dp"
                    android:textSize="14sp"
                    android:textStyle="normal"
                android:textColor="@color/entry_text"
                android:importantForAutofill="yes" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Password Text Field -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/password_field"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:boxBackgroundMode="outline"
                app:boxCornerRadiusTopStart="12dp"
                app:boxCornerRadiusTopEnd="12dp"
                app:boxCornerRadiusBottomStart="12dp"
                app:boxCornerRadiusBottomEnd="12dp"
                app:endIconMode="password_toggle"
                app:boxStrokeColor="@color/outline"
                app:hintTextColor="@color/hint_color"
                android:layout_marginBottom="16dp"
                android:hint="@string/hint_password">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/password_entry_field"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:inputType="textPassword"
                    android:paddingStart="12dp"
                    android:drawableStart="@drawable/password_protection"
                    android:drawablePadding="10dp"
                    android:textStyle="normal"
                    android:textColor="@color/entry_text"
                android:textSize="14sp"
                android:importantForAutofill="yes" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Primary Sign Up Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_sign_up"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="Sign Up"
                android:textAllCaps="false"
                android:textSize="16sp"
                app:cornerRadius="24dp"
                android:layout_marginBottom="8dp"
                android:fontFamily="@font/inter18ptsemibold"
                android:backgroundTint="@color/primary"
                android:textColor="@color/onPrimary" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Custom Google sign-in button -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/google_sign_up_button"
        android:layout_width="0dp"
        android:layout_height="52dp"
        app:layout_constraintTop_toBottomOf="@id/sign_up_detail_field"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="26dp"
        android:clickable="true"
        app:strokeColor="#DADCE0"
        app:strokeWidth="2dp"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:backgroundTint="@color/white">

        <LinearLayout
            android:id="@+id/google"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="14dp"
            android:paddingEnd="16dp">

            <ImageView
                android:id="@+id/google_icon"
                android:layout_width="45dp"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp"
                android:layout_height="35dp"
                android:layout_marginStart="10dp"
                android:src="@drawable/g_logo" />

            <TextView
                android:id="@+id/google_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp"
                android:layout_marginStart="10dp"
                android:fontFamily="@font/inter18ptmedium"
                android:gravity="center"
                android:text="Sign up with Google"
                android:textColor="@color/primaryText"
                android:textSize="20sp" />                 <!-- fixed -->
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/sign_up_sign_in_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/google_sign_up_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/signin_text"
        android:gravity="center"
        android:clickable="true"
        android:textSize="18sp"
        android:layout_margin="40dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
